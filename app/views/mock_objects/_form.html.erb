<%= form_for(@mock_object) do |f| %>
  <% if @mock_object.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mock_object.errors.count, "error") %> prohibited this mock_object from being saved:</h2>

      <ul>
      <% @mock_object.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
